const username = "Akif";
const helloMessage = `Hello ${username}`;
// const helloMessage = "Hello " + name; //without template literals


console.log(helloMessage);


const numberOfBooks = 4;
const bookPrice = 12;
const currency = "TL";
const totalPriceMessage = `Total price is ${bookPrice * numberOfBooks} ${currency}`;
//const totalPriceMessage = "Total price is " + bookPrice * numberOfBooks + " " + currency; //without template literals


console.log(totalPriceMessage);

